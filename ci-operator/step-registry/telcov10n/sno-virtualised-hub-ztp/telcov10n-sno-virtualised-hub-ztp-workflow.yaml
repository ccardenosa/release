workflow:
  as: telcov10n-sno-virtualised-hub-ztp
  steps:
    pre:
    - ref: telcov10n-sno-virtualised-hub-ztp-deploy-hub
    - ref: telcov10n-sno-hub-ztp-share-hub-cluster-profile
    post:
    - ref: wait
    env:
      TIMEOUT: "+10 hour"
      PR_ONLY: "true"
  documentation: |-
    Install a virtualised hub cluster using KCLI wrapper ansible role.
